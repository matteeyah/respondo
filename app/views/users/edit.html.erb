<% @page_title = "Profile" %>

<div class="grid lg:grid-cols-2 gap-5">
  <div class="card">
    <span class="font-medium">Add accounts</span>

    <div class="flex flex-col gap-3" id="account-providers">
      <%= render partial: "shared/account_provider",
                  collection: (UserAccount.providers.keys - @user.accounts.map(&:provider)),
                  as: :account_provider %>
    </div>
  </div>
  <div class="card">
    <span class="font-medium">Existing accounts</span>

    <%= render partial: "shared/accounts", locals: { model: @user } %>
  </div>
</div>
