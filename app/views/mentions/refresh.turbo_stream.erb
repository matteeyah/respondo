<%= turbo_stream.append(:toasts, partial: "shared/toast",
                                 locals: { success: true,
                                           message: "Mentions will be loaded asynchronously. Refresh the page to see new mentions once they load." }) %>
