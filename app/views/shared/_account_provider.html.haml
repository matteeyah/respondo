.list-group-item
  .row
    .col-8.d-flex.align-items-center
      = bi_icon(provider, 'fs-4 me-2')
      %span.fw-bold.my-0= provider_human_name(provider)
    .col-4.d-flex.justify-content-end
      = auth_provider_link(provider, model.class.to_s.downcase, { class: 'btn btn-outline-primary' }) do
        Connect
