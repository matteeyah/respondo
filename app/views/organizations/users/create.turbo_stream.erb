<%= turbo_stream.append(:alerts, partial: "shared/alert",
                                 locals: { success: true, message: "User was successfully added to the organization." }) %>
<%= turbo_stream.append("users-in-organization", partial: "organizations/user", locals: { user: @user }) %>
