<div class="flex flex-col items-center">
  <h2>Woohoo, you're in!</h2>
  <p>The next step is to authorize a organization account.</p>
  <hr class="w-1/2 my-5 text-stone-300">
</div>
<div class="flex justify-center gap-3">
  <div>
    <%= auth_provider_link("twitter", { class: "button" }) do %>
      Authorize Twitter
    <% end %>
  </div>
  <div>
    <%= auth_provider_link("linkedin", { class: "button" }) do %>
      Authorize LinkedIn
    <% end %>
  </div>
</div>
