%ul.list-group
  - tickets.each do |ticket, replies|
    %li.list-group-item.pr-0.border-right-0.border-top-0.border-bottom-0
      = turbo_frame_tag dom_id(ticket) do
        = render partial: 'ticket', locals: { brand:, ticket: }

        - if replies.any?
          = render partial: 'tickets', locals: { brand:, tickets: replies }
