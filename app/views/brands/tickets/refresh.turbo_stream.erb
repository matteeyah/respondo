<%= turbo_stream.append(:toasts, partial: 'shared/toast',
                                 locals: { success: true,
                                           message: 'Tickets will be loaded asynchronously. Refresh the page to see new tickets once they load.' }) %>
