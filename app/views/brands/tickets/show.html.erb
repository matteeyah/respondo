<% ticket = @ticket_hash.first.first %>
<%= turbo_frame_tag dom_id(ticket) do %>
  <%= render partial: 'ticket', locals: { brand: ticket.brand, ticket:, action_form: @action_form } %>
  <%= render partial: 'tickets', locals: { brand: ticket.brand, tickets: @ticket_hash.first.second } %>
<% end %>
